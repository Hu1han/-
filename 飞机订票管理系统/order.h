#pragma once
#include"search.h"

void modify(string address,string goal, string mod);

void order(vector<flight> f_t);

string reinformation(string name,string reinf, bool is_order=true);

bool is_legal(string s);

int to_int(string s);

